apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crossplane-example.fullname" . }}-test-connection"
  labels:
    {{- include "crossplane-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crossplane-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
